apiVersion: v1
kind: Service
metadata:
  name: mlbrFtPrnts-mysql
  labels:
    app: mlbrFtPrnts
spec:
  ports:
    - port: 3306
  selector:
    app: mlbrFtPrnts
    tier: mysql
  clusterIP: None
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlbrFtPrntsDb-pv-claim
  labels:
    app: mlbrFtPrnts
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: mlbrFtPrnts-mysql
  labels:
    app: mlbrFtPrnts
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mlbrFtPrnts
        tier: mysql
    spec:
      containers:
      - image: mysql:8.0.11
        name: mysql
        env:
          # $ kubectl create secret generic mysql-pass --from-file=password.txt
          # make sure password.txt does not have a trailing newline
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-pass
              key: password.txt
        ports:
        - containerPort: 3306
          name: mysql
        volumeMounts:
        - name: mysql-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-persistent-storage
        persistentVolumeClaim:
          claimName: mysql-pv-claim